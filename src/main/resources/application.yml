spring:
  application:
    name: Arquitetura Spring
  jpa:
    show_sql: true
  main:
    lazy-initialization: false

app:
  montadora:
    motor-padrao: 130
  config:
    variavel: valor da variavel x
